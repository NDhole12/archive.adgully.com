# === COPY AND RUN THIS COMMAND ===

ssh root@31.97.233.171 "apt update && apt install -y software-properties-common && add-apt-repository -y ppa:ondrej/php && apt update && DEBIAN_FRONTEND=noninteractive apt install -y php8.2-fpm php8.2-mysql php8.2-curl php8.2-gd php8.2-mbstring php8.2-xml php8.2-zip && systemctl enable php8.2-fpm && systemctl start php8.2-fpm && systemctl restart nginx && echo 'DONE - Check https://archive2.adgully.com/'"

Password: z(P5ts@wdsESLUjMPVXs

# This will:
# 1. Install PHP 8.2 (if missing)
# 2. Start PHP-FPM service
# 3. Restart Nginx
# 4. Fix the site

# Takes 2-3 minutes
# After it completes, site will work
